@extends('layouts.app')
@section('content')
<div class="container todo-page"><div class="todo-heading"><div class="d-flex align-items-start gap-3"><span class="todo-page-icon"><i class="bi bi-check2-square"></i></span><div><div class="todo-eyebrow">WORK MANAGEMENT</div><h1>To-do List</h1><p>Kelola pekerjaan, deadline, PIC, dan progres eksekusi dari seluruh campaign.</p></div></div><a href="{{ route('todo-list.create') }}" class="btn btn-brand"><i class="bi bi-plus-lg"></i>To-do Baru</a></div><div class="todo-stat-grid"><div class="todo-stat total"><span>Total Task</span><strong>{{ $summary['total'] }}</strong><small>semua pekerjaan</small></div><div class="todo-stat todo"><span>To do</span><strong>{{ $summary['todo'] }}</strong><small>belum dimulai</small></div><div class="todo-stat progress"><span>In progress</span><strong>{{ $summary['in_progress'] }}</strong><small>sedang dikerjakan</small></div><div class="todo-stat done"><span>Done</span><strong>{{ $summary['done'] }}</strong><small>telah selesai</small></div></div><div class="card todo-filter"><div class="card-body"><form method="GET" class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label">Cari Task</label><input name="search" value="{{ $search }}" class="form-control" placeholder="Judul task atau campaign"></div><div class="col-md-2"><label class="form-label">Status</label><select name="status" class="form-select"><option value="">Semua</option>@foreach(['todo'=>'To do','in_progress'=>'In progress','done'=>'Done'] as $key => $label)<option value="{{ $key }}" @selected($status === $key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Prioritas</label><select name="priority" class="form-select"><option value="">Semua</option>@foreach(['urgent'=>'Mendesak','high'=>'Tinggi','normal'=>'Normal','low'=>'Rendah'] as $key => $label)<option value="{{ $key }}" @selected($priority === $key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-brand">Terapkan</button><a href="{{ route('todo-list.index') }}" class="btn btn-outline-secondary">Reset</a></div></form></div></div><div class="todo-table card"><div class="card-header"><strong><i class="bi bi-list-check"></i>Daftar Pekerjaan</strong><span>Terurut berdasarkan urgensi dan deadline</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Task</th><th>Campaign</th><th>Status</th><th>Prioritas</th><th>PIC</th><th>Deadline</th><th>Aksi</th></tr></thead><tbody>@forelse($tasks as $task)<tr><td><strong>{{ $task->title }}</strong><small>{{ $task->description ?: 'Tidak ada deskripsi' }}</small></td><td><a class="todo-campaign-link" href="{{ route('campaigns.show', $task->campaign) }}">{{ $task->campaign->name }}</a></td><td><span class="todo-badge todo-status-{{ $task->status }}">{{ ['todo'=>'To do','in_progress'=>'In progress','done'=>'Done'][$task->status] }}</span></td><td><span class="todo-badge todo-priority-{{ $task->priority }}">{{ ['low'=>'Rendah','normal'=>'Normal','high'=>'Tinggi','urgent'=>'Mendesak'][$task->priority] }}</span></td><td>{{ $task->assignee->name ?? 'Tanpa PIC' }}</td><td>{{ $task->due_date?->format('d M Y') ?? '-' }}</td><td class="text-nowrap"><a href="{{ route('todo-list.edit', $task) }}" class="btn btn-sm btn-outline-primary"><i class="bi bi-pencil"></i>Edit</a><form method="POST" action="{{ route('todo-list.destroy', $task) }}" class="d-inline" onsubmit="return confirm('Hapus task ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash3"></i></button></form></td></tr>@empty<tr><td colspan="7" class="todo-empty">Belum ada pekerjaan. Tambahkan To-do pertama.</td></tr>@endforelse</tbody></table></div><div class="table-pagination">{{ $tasks->links() }}</div></div></div>
@endsection
