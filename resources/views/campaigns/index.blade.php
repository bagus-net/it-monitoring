@extends('layouts.app')
@section('content')
<div class="container campaign-page"><div class="campaign-heading"><div class="d-flex align-items-start gap-3"><span class="campaign-page-icon"><i class="bi bi-megaphone"></i></span><div><div class="campaign-eyebrow">IT CAMPAIGN STUDIO</div><h1>Campaign</h1><p>Kelola program, target, PIC, dan progres campaign dalam satu workspace.</p></div></div><a href="{{ route('campaigns.create') }}" class="btn btn-brand"><i class="bi bi-plus-lg"></i>Campaign Baru</a></div>
<div class="campaign-stat-grid"><div class="campaign-stat stat-total"><span>Total Campaign</span><strong>{{ $summary['total'] }}</strong><small>semua campaign aktif dan arsip</small></div><div class="campaign-stat stat-active"><span>Active</span><strong>{{ $summary['active'] }}</strong><small>sedang berjalan</small></div><div class="campaign-stat stat-planned"><span>Planned</span><strong>{{ $summary['planned'] }}</strong><small>siap dieksekusi</small></div><div class="campaign-stat stat-completed"><span>Completed</span><strong>{{ $summary['completed'] }}</strong><small>berhasil diselesaikan</small></div></div>
<div class="card campaign-filter"><div class="card-body"><form method="GET" class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label">Cari Campaign</label><input name="search" value="{{ $search }}" class="form-control" placeholder="Nama, channel, atau audiens"></div><div class="col-md-3"><label class="form-label">Status</label><select name="status" class="form-select"><option value="">Semua Status</option>@foreach(['planned'=>'Planned','active'=>'Active','paused'=>'Paused','completed'=>'Completed','archived'=>'Archived'] as $key => $label)<option value="{{ $key }}" @selected($status === $key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-4 d-flex gap-2"><button class="btn btn-brand">Terapkan</button><a href="{{ route('campaigns.index') }}" class="btn btn-outline-secondary">Reset</a></div></form></div></div>
<div class="campaign-grid">@forelse($campaigns as $campaign)<article class="campaign-card"><div class="campaign-card-top"><span class="campaign-status status-{{ $campaign->status }}">{{ ucfirst($campaign->status) }}</span><span class="campaign-code">{{ strtoupper(substr($campaign->slug, 0, 12)) }}</span></div><h2>{{ $campaign->name }}</h2><p>{{ $campaign->objective ?: 'Objective belum ditulis.' }}</p><div class="campaign-progress"><div class="campaign-progress-label"><span>Progress</span><strong>{{ $campaign->progress }}%</strong></div><div class="campaign-progress-track"><i style="width:{{ $campaign->progress }}%"></i></div></div><div class="campaign-card-meta"><span><i class="bi bi-calendar3"></i>{{ $campaign->start_date?->format('d M Y') ?: 'Tanpa tanggal' }} - {{ $campaign->end_date?->format('d M Y') ?: 'Terbuka' }}</span><span><i class="bi bi-person"></i>{{ $campaign->owner->name ?? 'Belum ada PIC' }}</span><span><i class="bi bi-broadcast"></i>{{ $campaign->channel ?: 'Channel belum diisi' }}</span></div><div class="campaign-card-footer"><small>{{ $campaign->target_value ? number_format($campaign->achieved_value, 0, ',', '.') . ' / ' . number_format($campaign->target_value, 0, ',', '.') . ' ' . ($campaign->target_unit ?: '') : 'Target belum ditentukan' }}</small><div><a href="{{ route('campaigns.show', $campaign) }}" class="btn btn-sm btn-outline-secondary"><i class="bi bi-eye"></i>Detail</a><a href="{{ route('campaigns.edit', $campaign) }}" class="btn btn-sm btn-outline-primary"><i class="bi bi-pencil"></i>Edit</a></div></div></article>@empty<div class="campaign-empty"><i class="bi bi-megaphone"></i><strong>Belum ada campaign</strong><span>Buat campaign pertama untuk mulai mengelola target dan progres.</span><a href="{{ route('campaigns.create') }}" class="btn btn-brand">Buat Campaign</a></div>@endforelse</div>
<div class="table-pagination">{{ $campaigns->links() }}</div></div>
@endsection
