@extends('layouts.app')

@section('content')
<div class="container mt-4 recycle-bin-page"><div class="d-flex justify-content-between align-items-start gap-3 mb-3"><div><div class="recycle-kicker">MASTER ADMIN</div><h2 class="mb-1">Sampah Data</h2><p class="text-muted mb-0">Pulihkan data yang terhapus atau hapus permanen bila tidak lagi diperlukan.</p></div><div class="recycle-count">{{ $records->count() }} data terhapus</div></div><div class="card recycle-card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Jenis Data</th><th>Data</th><th>Waktu Dihapus</th><th>Aksi</th></tr></thead><tbody>@forelse($records as $record)<tr><td><span class="recycle-type">{{ $record['label'] }}</span></td><td><strong>{{ $record['name'] }}</strong></td><td>{{ $record['deleted_at']->format('d M Y H:i') }}</td><td class="text-nowrap"><form method="POST" action="{{ route('recycle-bin.restore', [$record['type'], $record['id']]) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success">Pulihkan</button></form><form method="POST" action="{{ route('recycle-bin.force-delete', [$record['type'], $record['id']]) }}" class="d-inline" onsubmit="return confirm('Hapus permanen data ini? Tindakan ini tidak dapat dibatalkan.')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus Permanen</button></form></td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-5">Sampah Data kosong.</td></tr>@endforelse</tbody></table></div></div></div>
<style>.recycle-kicker{font-size:.7rem;font-weight:800;letter-spacing:.12em;color:#0b5ea8}.recycle-count{padding:8px 12px;background:#e0f2fe;color:#075985;border-radius:4px;font-size:.82rem;font-weight:700}.recycle-card{border:1px solid #dbe5ef}.recycle-card thead th{background:#f8fafc;color:#475569;font-size:.76rem;text-transform:uppercase}.recycle-type{display:inline-block;padding:4px 7px;border-radius:4px;background:#f1f5f9;color:#475569;font-size:.75rem;font-weight:700}</style>
@endsection
