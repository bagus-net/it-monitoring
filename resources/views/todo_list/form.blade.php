<div class="row g-3"><div class="col-md-6"><label class="form-label">Campaign</label><select name="campaign_id" class="form-select" required><option value="">-- Pilih Campaign --</option>@foreach($campaigns as $campaign)<option value="{{ $campaign->id }}" @selected((string) old('campaign_id', $task?->campaign_id) === (string) $campaign->id)>{{ $campaign->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Judul To-do</label><input name="title" class="form-control" value="{{ old('title', $task?->title) }}" placeholder="Contoh: Review materi campaign" required></div><div class="col-md-4"><label class="form-label">Status</label><select name="status" class="form-select"><option value="todo" @selected(old('status', $task?->status ?? 'todo') === 'todo')>To do</option><option value="in_progress" @selected(old('status', $task?->status) === 'in_progress')>In progress</option><option value="done" @selected(old('status', $task?->status) === 'done')>Done</option></select></div><div class="col-md-4"><label class="form-label">Prioritas</label><select name="priority" class="form-select"><option value="low" @selected(old('priority', $task?->priority ?? 'normal') === 'low')>Rendah</option><option value="normal" @selected(old('priority', $task?->priority ?? 'normal') === 'normal')>Normal</option><option value="high" @selected(old('priority', $task?->priority) === 'high')>Tinggi</option><option value="urgent" @selected(old('priority', $task?->priority) === 'urgent')>Mendesak</option></select></div><div class="col-md-4"><label class="form-label">PIC</label><select name="assignee_id" class="form-select"><option value="">Tanpa PIC</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected((string) old('assignee_id', $task?->assignee_id) === (string) $user->id)>{{ $user->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Deadline</label><input type="date" name="due_date" class="form-control" value="{{ old('due_date', $task?->due_date?->format('Y-m-d')) }}"></div><div class="col-12"><label class="form-label">Deskripsi</label><textarea name="description" class="form-control" rows="3" placeholder="Detail pekerjaan atau acceptance criteria...">{{ old('description', $task?->description) }}</textarea></div></div>
